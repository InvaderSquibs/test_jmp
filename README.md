# test_jmp

Roost visualization prototype deliverables:

- Checkpoint 1: plan + replay event schema in `docs/roost-viz-plan.md`
- Checkpoint 2: art constraints + sprite pipeline + placeholder atlas in `docs/roost-art-pipeline.md` and `assets/atlas/*`
- Checkpoint 3: replay UI prototype (Map + StoryPanel + Inspector) with 2 demo replays in `prototype/`

## Run prototype

```bash
cd prototype
python3 -m http.server 4173
```

Open <http://localhost:4173>.
